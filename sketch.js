function setup() {
  createCanvas(600, 400);
  background(225); //fundo branco
  
  //Círculo  vermelho no centro
  noStroke()
  fill (215, 0, 58); //cor vermelha
  ellipse(width / 2, height / 2, 240, 240);
}